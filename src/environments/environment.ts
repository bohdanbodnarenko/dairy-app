// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBOh3PG006VvrKZRo-G-O2LHRV4-wtzMoc",
    authDomain: "dairy-app-6b5be.firebaseapp.com",
    databaseURL: "https://dairy-app-6b5be.firebaseio.com",
    projectId: "dairy-app-6b5be",
    storageBucket: "dairy-app-6b5be.appspot.com",
    messagingSenderId: "109250009911"
  }
};

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
